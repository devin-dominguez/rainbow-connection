
import { colors } from 'frontend/helpers/colors';
import { module, test } from 'qunit';

module('Unit | Helper | colors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colors([42]);
  assert.ok(result);
});

